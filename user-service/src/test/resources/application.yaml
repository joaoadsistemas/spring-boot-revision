jpa:
  show-sql: true
  properties:
    hibernate:
      globally_quoted_identifiers: true
  hibernate:
    ddl-auto: update
server:
  error:
    include-binding-errors: always
    include-message: always